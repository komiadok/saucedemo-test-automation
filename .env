# Application URL
BASE_URL=https://www.saucedemo.com/

# Browser
BROWSER=chrome      # navigateur à utiliser pour les tests (chrome, firefox, edge)
HEADLESS=true       # mode sans interface graphique

# Timeouts 
DEFAULT_TIMEOUT=10  # délai d'attente par défaut pour les éléments (en secondes)